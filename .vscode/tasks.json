{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build malloc_test",
            "type": "shell",
            "command": "cd ${workspaceFolder}/test && gcc -g3 -O0 malloc_test.c -o malloc_test",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}